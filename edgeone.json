{
  "name": "calculus-formula-theater-spa",
  "version": "1.0.0",
  "build": {
    "buildCommand": "cd app && npm install && npm run build",
    "outputDir": "app/dist",
    "installCommand": "cd app && npm install"
  }
}
