# 2026-08-20 工作日志

## 调用 harness-creator 技能，为「面容整理」建立编码 agent harness
- 目标：把项目架构、目录结构、以及「内容增量工作流」固化成 agent 可读的 harness，约束：只增量添加，绝不改动现有页面架构/设计/风格。
- 重写 `AGENTS.md`：新增 §1 项目架构与目录结构、§4「🔑 内容增量工作流（铁律）」五步流程（笔记先写 → 用户审核 → index.html 增量 → extractFormulas → review 自动同步），并修正模块集合（无 `diff`，微分并入 `deriv`）。
- 新建 `feature_list.json`（6 模块状态 + 不变量）、`progress.md`（状态/证据/下一步）、`init.sh`（验证门禁，跑 extractFormulas 并校验 ≥176 条、6 模块齐全）、`session-handoff.md`（跨会话交接）。
- 验证：`node scripts/extractFormulas.mjs` → 176 条公式，6 模块（deriv 87 / integral 19 / trig 41 / diffEq 9 / linalg 7 / limit 13）；`bash init.sh` 通过。
- 未改动 index.html / review.html / formulas.js 既有内容（仅重新生成 formulas.js 验证）。
- 16:26 补充澄清第1步笔记协作规则：人工先给初版（覆盖哪些内容/公式），AI 按用户个性化要求润色成风格一致的笔记；偏好不明先提问。已同步到 AGENTS.md §4 第1步、数据流图、口诀，以及 session-handoff.md。

## 新增内容：notes/08-矩阵.md（初版→AI润色）
- 用户提供「矩阵」初版要点（特殊矩阵/四则运算/转置/方阵行列式/逆与伴随/初等变换/秩），确认只写笔记（不动页面）。详略=公式+推导/例题，加 ⭐考点/⚠️易错 标注。
- 已写 `notes/08-矩阵.md`：风格对齐 07-行列式（知识地图+一/二/三章节+公式块+口诀+表格+例题+速查总表+核心记忆口诀），含二阶伴随「主对调副变号」、AA*=|A|E 推导（引用 07 展开逆用）、初等行变换求逆 (A|E)→(E|A⁻¹) 及推广、秩=行阶梯级数、满秩⇔|A|≠0。
- 已在 `notes/00-笔记索引.md` 登记第 07 行（序号沿用：06=行列式，07=矩阵，文件名为 08-矩阵.md），学习路径补充矩阵模块说明。
- **状态：笔记草稿待用户审核，尚未进入 index.html / review.html**（按工作流第2步）。
- 18:32 用户审核反馈：单位阵/对称/反称要用具体数字矩阵形状（非纯字母）；加/减/数乘各补一个例题，乘法补数字例题。已修订 08-矩阵.md：① 1.1 单位阵补 $E=\begin{pmatrix}1&0&0\\0&1&0\\0&0&1\end{pmatrix}$ 及 $EA=A$ 验证例题；② 1.2 对角阵补具体数字形状；③ 1.3 对称补 $\begin{pmatrix}1&2&3\\2&4&5\\3&5&6\end{pmatrix}$；④ 1.4 反称补 $\begin{pmatrix}0&2&3\\-2&0&4\\-3&-4&0\end{pmatrix}$；⑤ 2.1 加/减/数乘各补 1 例题（含⭐真题）；⑥ 2.2 乘法补 $2\times3\times3\times2$ 具体计算例题。仍未动页面。
- 18:36 用户批评：放题前应先看记忆/既有笔记惯例。问题根因——我把两个随手编的普通例题错误标成「⭐真题」（无真实出处），而本项目的真题惯例是必须带年份+地区+题号（见现有 notes）。已修正：去掉 2.1 加法、数乘例题的「⭐真题」标签，改回普通「例题」。已新建 `.workbuddy/memory/MEMORY.md` 固化铁律：「真题必须有真实出处，否则只能叫例题」。
- 18:37 用户要求：去网上搜真实真题，不要自己编。已用 WebSearch 搜到并核实多道真实 专升本 矩阵 真题（跨浙江/山西/重庆/四川卷，答案一致可验证），插入 08-矩阵.md 对应小节：① §6.2 重庆2017 二阶求逆 A=[[2,-1],[-5,3]]；② §6.3 重庆2017 可逆条件选择 |A|≠0；③ §6.5 浙江2025 逆矩阵 A=[[1,2],[3,4]] + 山西2025 第21题 (AB)⁻¹；④ §8.2 四川专升本 求秩 r(A)=3（4×3 矩阵，已手算验证）。所有真题均带来源+完整解，未伪造题号。仍未动页面。
- 注意（已被推翻）：矩阵进页面时曾误并入现有 `buildLinearDetail()`（标题改「行列式 + 矩阵」），未新增模块。此做法**错误**——行列式与矩阵应为两个独立章节。见下方 19:33 修正记录。

## 矩阵笔记审核通过 → 增量进页面 + 翻卡同步（19:xx）
- 用户回复「审核通过」，按工作流第 3–5 步执行：在 `src/index.html` 的 `buildLinearDetail()` 内**增量新增 5 个 tab**——`mat-special`(特殊矩阵)、`mat-op`(矩阵运算)、`mat-inv`(逆·伴随·性质)、`mat-elem`(初等变换与秩)、`mat-summary`(矩阵速查公式卡)。
- 内容全部复用现有 CSS 类（detail-tab/detail-section/section-title/groupTitle/memoryBox/formulaCard），未改既有行列式 tabs、未改设计/风格/架构；标题由「行列式框架详情」改为「线性代数详情（行列式 + 矩阵）」。
- 新增 29 条 `formulaCard`（单位阵/对角/对称/反称/加减运算/乘法/消去陷阱/方阵多项式/转置/方阵行列式/逆/伴随/核心等式/逆伴随关系/初等行变换/求逆/秩 等），含具体数字形状（$E=\begin{pmatrix}1&0&0\\0&1&0\\0&0&1\end{pmatrix}$ 等）与 LaTeX 注释。
- 跑 `node scripts/extractFormulas.mjs` → 抽出 **205 条**（linalg 7→**36**，+29 矩阵），6 模块齐全；`formulas.js` 已重写，`review.html` 翻卡自动同步（遍历 FORMULAS 按 module 过滤）。
- 校验：抽出主脚本块跑 `node --check` → SYNTAX_OK（228KB 完整可解析）；formulas.js 中矩阵卡 LaTeX 转义正确（`\begin{pmatrix}...\end{pmatrix}` 单反斜杠、`\\` 双反斜杠换行）。
- 已同步更新 `feature_list.json`(linalg 36 / totals 205)、`progress.md`(证据更新)、`MEMORY.md`(模块事实 + 矩阵集成方式说明)。
- 未提交 git（用户未要求）。下一步若需要可 `npm run dev` 人工核对双主题渲染，或提交。
- 19:0x 用户批量细化 08-矩阵.md：① 2.3 消去陷阱补 ⭐真题（2025《线性代数(专升本)》试题，AB=AC 选 D |A|≠0时B=C）；② 三、方阵多项式补有难度例题 f(A)=A²-5A+3E（A=[[2,1],[-1,3]]→-4E）；③ 四、转置补「是什么+具体形状+例题」及 (AB)ᵀ 逆序律例题；④ 五、方阵行列式补「是什么+2×2/3×3 例题」；⑤ 6.1 逆矩阵补具体形状（AA⁻¹=E 验证）+例题；⑥ 6.2 伴随补逐个代数余子式求 A* 的具体形状；⑦ 6.4 核心等式补验证例题 + ⭐真题（2025 专升本高代 |A*|=|A|ⁿ⁻¹ 选 C）；⑧ 7.1 三类初等行变换各补一例；⑨ 7.2 补 ⭐真题（四川·西华大学·理工 2021 求逆，完整行变换过程，A⁻¹=[[2,-1,-1],[3,-1,-2],[-1,1,1]]）。所有新增真题均带来源且答案经手算核实。仍未动页面。
- 已把「例题优先真题、找不到才用有难度例题」写入 MEMORY.md 取材规则章节。

## 修正：矩阵从「并入行列式」拆为独立章节 module=matrix（19:33）
- 用户批评：「新开一章啊，你他妈放行列式里面干什么？」——正确做法是矩阵独立成章，而非塞进 buildLinearDetail。
- 用 Node 脚本按唯一标记精确切分（`scripts/_split_matrix.cjs`，事后已删除）：把 `buildLinearDetail` 内的矩阵 tab/body 抽出，生成独立的 `buildMatrixDetail()`（5 tab：特殊矩阵/矩阵运算/逆·伴随/初等变换与秩/矩阵速查，29 条 formulaCard 不变）。`buildLinearDetail` 还原为仅行列式（标题回「行列式框架详情」，5 个行列式 tab）。
- 入口与分发同步：① path-home 新增「矩阵」行（enterDetail('matrix')，线性代数分类）；② swiper 卡片新增 08 矩阵 slide；③ 详情页底部相关卡新增「矩阵模块」；④ `buildDetailContent` switch 加 `case 'matrix': return buildMatrixDetail();`。
- `extractFormulas.mjs` 同步：MODULE_ORDER 加 'matrix'；sections 加 `{ name:'matrix', module:'matrix', title:'线性代数·矩阵' }`（自动识别 buildMatrixDetail）；注释 module 集合更新。跑脚本 → **205 条 / 7 模块**（linalg 回到 7，matrix 29；6 模块基线 176 + 矩阵 29）。
- `review.html` 自动同步（导入 MODULES/FORMULAS，按 module 过滤）；formulas.js 导出 MODULES 含 matrix(29)。
- 验证：主脚本 `node --check` → SYNTAX_OK；dev server 200；服务端确认 29 张 matrix 卡 + 首页 `enterDetail('matrix')`；buildLinearDetail 内已无 mat- 残留（10 处 mat- 引用全在 buildMatrixDetail 3624–3850）。
- 同步更新 `AGENTS.md`(模块映射表 + 计数 + 模块集合说明)、`feature_list.json`(linalg 7 / matrix 29 / totals 205 / 7 模块)、`MEMORY.md`(模块事实 + 「行列式≠矩阵，切勿再并入」铁律)、`progress.md`(证据更新)。
- 教训固化：行列式与矩阵是 notes 里 06/07 两章，index 也必须是两个独立入口；本项目的「增量」指同章节内追加，不等于把不同主题合并进一个模块。

- 19:55 用户反馈**翻卡页 linalg 模块「行5 · 异行为 0」卡片问侧变红**（截图显示 `\sum_{k=`、KaTeX 错误态红字）。修复过程：
  - 排除了：index.html 公式源、抽提结果、LaTeX 本体（用 katex.renderToString 单测 OK，生成 4335 chars HTML）；CSS overflow；坐标色变。
  - **根因**：`src/review.html` 的 `toQuestionFormula(formula)` 用了 `tex.indexOf('=')` 找主等号，撞到了 `\sum_{k=1}^{n}` 下标里的 `k=1` 的 `=`，把主式截成 `\sum_{k= ?`，KaTeX 报 `Expected '}'` 报错。
  - **修复**：line 1523– 改为 bracket-depth 扫描（遇 `{` depth++，`}` depth--，`\\` 跳下一字符），只把 depth=0 的「主等号」当分界点。
  - 回归测试 row1–row7 全部正确生成 `<左式> = ?` 形态；行5 现显示 `\sum_{k=1}^{n} a_{ik} A_{jk} = ?`（KaTeX OK）。
  - **经验**：处理 LaTeX 字符串别用朴素字符串操作假设 token 边界，`=` 经常出现在花括号参数里。已在 MEMORY.md 「翻卡页 LaTeX 已知坑」章节固化。
