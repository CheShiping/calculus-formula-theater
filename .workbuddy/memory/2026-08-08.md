# 2026-08-08

## 项目预览方式（面容整理 Web App）
- `review.html` 使用 ES Module 导入数据（`import { FORMULAS, MODULES } from './data/formulas.js'`），**必须通过 HTTP 服务器访问**，直接双击 `file://` 会因 CORS 白屏。
- 启动命令（在 src 目录）：`python -m http.server 8000 --bind 127.0.0.1`
  - 路径：E:/Users/Shiping/面容整理/src
  - 主页：/index.html，翻卡复习页：/review.html，数据：/data/formulas.js
- 已用 managed python (3.13.12) 在后台启动验证，所有资源返回 200。

## index.html 移动端顶部按钮隐藏
- 用户反馈：首页在移动端时，右上角"翻卡记忆 / GitHub / 主题切换"三个按钮遮挡下方章节标签。
- 解决：在 `@media (max-width: 768px)` 块内将 `.top-actions { display: none; }`，直接隐藏整个容器（取代之前的缩小样式）。
- 影响：移动端失去翻卡记忆/主题切换入口；用户接受此取舍。

## index.html 详情内容页：添加浮动标题栏 + 回到顶部按钮
- 浮动标题栏：已有的 `.detail-header`（`position: sticky; top: 0; z-index: 30`）在 #detail-view 滚动容器内固定在顶部，带返回按钮和章节 tabs。移动端生效（sticky 不受 flex-wrap 影响）。
- 回到顶部按钮：右下角圆形按钮 `.back-to-top-btn`
  - CSS：position fixed, opacity 0, 滚动 >400px 时 .visible 淡入
  - JS：监听 detailView scroll 事件，toggle .visible；点击 `detailView.scrollTo({top: 0, behavior:'smooth'})`
  - HTML：在 `</body>` 前添加按钮
- 与 review.html 的 back-to-top 逻辑一致（样式复用）

## index.html 移动端顶部按钮场景修正
- 首页轮播区：显示三个按钮（缩小版），可跳转翻卡页
- 详情内容页（#detail-view.active）：`body:has(#detail-view.active) .top-actions { display: none; }` 隐藏按钮
- 恢复原来的缩小样式（top/right/gap 等）

## index.html 纸张主题补全（点击按钮显示的内容统一）
- 问题：纸张主题仅覆盖 path-home 与 #detail-view 的头部/tab/卡片外壳；`.top-actions` 的纸张样式写在 `.path-home { }` 嵌套内（变成 `.path-home .top-actions`），但 top-actions 是 body 直接子节点、不在 path-home 内 → 顶栏实际仍是深色（未完成）。
- 详情内容（点击 path-node 后由 JS 构建）用了继承自 `:root` 的深色变量：`.formula-num`(--text-ghost 透明)、`.group-title`/`.chapter-flow-step` 边框(--border-soft 透明)、`.special-table`(边框/首列文字/背景)、六边形 SVG(--hex-vertex-fill 深底、--hex-label-color、--bg-elevated、--border-medium) 在纸张页上不可见/低对比。
- 修复：① 新增全局（非嵌套）`.top-actions` 纸张样式块（含移动端），顶栏真正统一为纸张主题；② 在 `#detail-view` 上重写 `--text-ghost/--text-muted/--text-dim/--text-secondary/--border-soft/--border-medium/--bg-elevated*/--hex-vertex-fill/--hex-label-color` 为纸张值，使详情内所有自定义元素（特殊值表、六边形、步骤、公式编号）一处统一样式。
- review.html 的纸张主题（顶栏/侧栏/chips/toolbar/翻卡/移动端分类栏/回顶）在 workspace 改动中已较完整，无需再改。

## 纸张主题二次同步（顶栏/按钮/回顶 三处）
- review.html 顶部栏 `.top-bar`（含 GitHub / 主题切换 / 翻卡 / 返回四个按钮）仍用深色 `var(--bg-sidebar)`，未同步纸张主题。新增全局覆盖块：`.top-bar a, .top-bar button` 纸色背景 `rgba(255,255,255,.52)` + 墨色文字 `#1e201d` + 细边框；`.top-action-review` 绿纸 `#e4f0e6`；hover 仅加深边框、文字保持墨色。
- review.html `.back-to-top-btn` 同步加全局纸张覆盖（此前仅 `[data-theme="light"]` 有纸色，深色模式仍是深色）。
- index.html 详情页隐藏首页顶栏规则 `body:has(#detail-view.active) .top-actions { display:none }` 原本只在 `@media (max-width:768px)` 内 → 桌面端不生效。新增全局（非媒体查询）版本，所有视口进入详情后首页顶栏均隐藏。
- index.html `.back-to-top-btn` 同样缺纸张覆盖（仍用 `var(--bg-sidebar)` 深色），新增全局纸色覆盖，与顶栏一致。

## 适配暗色主题 + 落地 Part1 动画
- 适配暗色主题：发现上一轮"强制纸色"打破了深色模式——review.html 默认是深色（`--bg-body:#000`），index.html 首页 `.path-home` 恒为纸色（`#fbf9f2`）但详情视图跟随主题。
  - review.html：删除强制纸色全局块，改为 `[data-theme="light"] .top-bar` 纸色覆盖；`.back-to-top-btn` 回到"默认深色表面 + `[data-theme="light"]` 纸色"的主题感知（原 light 覆盖仍在 979 行）。
  - index.html：`.top-actions` 因首页恒纸色保持纸色；`.back-to-top-btn` 由强制纸色改为 `[data-theme="light"]` 纸色覆盖（深色详情视图用默认深色表面）。
- Part1 动画（6 条全落地，复用共享缓动 `cubic-bezier(0.25,0.8,0.25,1)`，并加 `prefers-reduced-motion` 降级）：
  - index.html：① `.path-row` 滚动渐显（`.js` 门控防闪烁 + `IntersectionObserver`，`rootMargin:'0px 0px -12% 0px'`、`threshold:0.15`，加 `.reveal`；减 0.55s）；② `.path-intro` 载入上浮（`@keyframes fadeUp` 0.5s）；③ `.top-actions a/button:active` 按下 `scale(0.95)`；JS `initScrollReveal()`。
  - review.html：④ `.flip-card` 成组错峰进入（`renderGrid` 渲染后双 rAF 加 `.enter`，stagger `min(i,12)*45ms`，`.js` 门控）；⑤ `.hero` 载入上浮（同 `fadeUp`）；⑥ `.top-bar a/button:active` 按下 `scale(0.95)`。注意 reveal 的 `transform` 放在 `.flip-card` 外层，翻面 `rotateY` 在内层 `.flip-card-inner`，互不干扰。
  - 两个页面都在 `<head>` 内联脚本加 `document.documentElement.classList.add('js')`。

## 暗色主题按钮修复 + 章节滚动渐显改双向（index.html）
- 根因：index.html 首页 `.path-home`、详情 `#detail-view`、顶栏 `.top-actions` 的纸色全部**硬编码**（未按主题区分），导致主题切换按钮点了没反应。且 `body{background:#fbf9f2}` 写死。
- 修复（把纸色限定到浅色，深色作为 CSS 默认）：
  - `body`/`#main-view`：默认 `#1b1a16`（暖暗，非纯黑），`[data-theme="light"]` 为 `#fbf9f2`。
  - `.path-home`：`--path-*` 深色值作为默认，纸色值放 `[data-theme="light"] .path-home`；背景渐变同样两套。
  - `.top-actions`：纸色限定到 `[data-theme="light"]`（深色用基础 `var(--bg-sidebar)`）。
  - `#detail-view` 整块纸色覆盖（约 891–1018 行）逐选择器加 `[data-theme="light"]` 前缀；深色回退到 329–460 的 `:root` 深色基础样式。`.special-table`/`.hexagon`/`.chapter-flow-step` 等靠 CSS 变量，天然跟随主题，无需改。
  - 默认主题改为浅色（纸张）：head 提前应用 + `initTheme` 末尾 `applyTheme('light')`，保证首次打开仍是纸张（不回退），切换按钮才真正生效。
- 双向滚动渐显：`initScrollReveal` 去掉 `io.unobserve`，改 `e.target.classList.toggle('reveal', e.isIntersecting)`（`rootMargin:'0px 0px -10% 0px'`、`threshold:0.12`），进入视口淡入、离开淡出，上下滚动对称。
- review.html：经排查其 body 与大量组件纸色硬编码且 `:root` 暗色是纯黑 #000（与用户"避免纯黑底"偏好冲突），本轮**回退**了对它的主题默认改动，保持纸张；review 若要做暗色需另做暖暗色变体。

## review.html 暖暗色主题 + 移动端三件套 + 两页移动端同步
- 暖暗色主题（review 支持暗色，非纯黑）：
  - `:root` 暗色改为暖暗色系（`--bg-body:#17140f`、`--bg-main-stage` 暖暗径向渐变、`--bg-sidebar:rgba(32,27,19,.72)`、暖白文字 `#f2ecdf`、暖色边框 `rgba(255,238,210,*)`）。
  - "复习页纸张舞台"块：把 `body {…纸色变量…}` 改为 `[data-theme="light"] body {…}`；把硬编码纸色的组件规则（top-bar/chip.active/toolbar.primary/know-btn/progress/flip-face/flip-face.back/mobile-category-bar/back-to-top 等）逐一加 `[data-theme="light"]` 前缀。用 var 的规则（sidebar/hero/chip base/flip-body/main-stage 等）保持未限定，天然跟随主题。深色回退到 247/314/437/562/685/742 等 var 化基础样式。
  - 默认浅色（纸张）：head 提前应用 + `loadTheme` 末尾 `applyTheme('light')`。两页 CSS 大括号校验平衡（review 206/206，index 293/293）。
- 移动端三件套（review.html，对应截图反馈）：
  - 文字平铺：根因是纸张块 `.sidebar-container{width:350px}`（约 866 行）覆盖了移动端 `width:100%`（794 行，同特异性但 866 在后）。在移动端块加 `[data-theme="light"] .sidebar-container{width:100%}`（993 块，更后）修复。
  - 分类栏按需显示：`.mobile-category-bar` 移动端默认 `opacity:0;translateY(-10px);pointer-events:none`，滚动时首个 `.flip-card` 的 `getBoundingClientRect().top<0` 才加 `.show`（`initMobileCategoryBarScroll`，监听 window scroll/resize，mq 限 ≤640px）；位置 `top:3.6rem` 置于顶栏下方避免重叠。
  - 返回按钮：移动端原本 `.top-bar{display:none}`（798）导致无返回；改为移动端显示缩小版 `.top-bar`（36px，top/right 0.6rem），并隐藏冗余的"当前页"`.top-action-review`，保留返回/GitHub/主题。
- 两页移动端同步：review 顶栏缩小版与 index 移动端 `.top-actions`（top:0.6rem/right:0.75rem/36px）对齐，两页移动端均有含返回/主题的紧凑顶栏。
